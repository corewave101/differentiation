# 미분의 실생활 활용 MVP 계획

## 현재 MVP

1. **상자와 물**
   - 밑면과 네 옆면의 총면적, 밑면 가로/세로 비율을 고정한 상자의 부피 `V(x)`를 만든다.
   - 높이 `x`에 따라 밑면을 자동 조절하고 `V'(x)=0`인 높이에서 최대 부피를 확인한다.
   - Three.js로 상자를 돌리고 물을 붓는 장면을 보여준다.
2. **배트와 날아가는 공**
   - 탑뷰에서 같은 경로를 이동하는 두 스윙을 가로로 비교한다.
   - 위치 함수를 미분한 순간 속력으로 임팩트 속도를 구하고, `J=mΔv`로 추정 충격량과 임팩트 뒤 공의 비행을 함께 보여준다.
3. **터치스크린**
   - 손가락 위치의 변화량을 시간으로 나눠 현재 속력을 계산한다.
   - 짧고 느린 입력은 터치, 중간 속도 이동은 드래그, 긴 거리·높은 속력은 스와이프로 분류한다.
   - 터치는 무지개 공, 드래그는 놓은 속도의 공, 스와이프는 더 빠른 발사로 연결한다.

## 현재 확장: 상자·원기둥·원뿔·구

- **상자**: 뚜껑 없는 상자의 종이 면적 `S=ab+2x(a+b)`와 밑면 비율 `a/b`를 고정한다. 높이 `x`에 따라 밑면을 자동 조절하며 부피 `V(x)`와 도함수 `V'(x)`를 비교한다.
- **원기둥**: 겉넓이 `400cm²`를 고정하고 반지름에 따른 높이·부피를 계산해 최대 부피 조건을 찾는다. 반지름 범위는 `3~7.9cm`로 제한한다.
- **원뿔**: 모선 `12cm`를 고정하고 반지름에 따른 높이·부피를 계산해 최대 부피 조건을 찾는다.
- **구**: `V(r)=4/3πr³`를 미분하면 `V'(r)=4πr²`, 즉 구의 표면적이 된다는 관계를 보여준다.
- UI는 상자 탭 안에 `상자 / 원기둥 / 원뿔 / 구` 선택 탭을 두고, 도형을 3D로 회전·채우기 하면서 부피와 도함수 변화를 확인한다.
- 물 채우기는 일정 유량 `Q=120cm³/s`를 사용한다. 수면은 항상 수평이며, 물 부피를 수면 높이로 역산해 `dV/dh=A(h)`, `dh/dt=Q/A(h)`를 반영한다. 원뿔은 꼭짓점이 위에 있는 방향이며, 구의 물 부피는 `πh²(R-h/3)`이다.
- 공통 흐름은 **조건 설정 → 함수 세우기 → 도함수 계산 → 최적값을 3D 장면에서 확인**으로 유지한다.
